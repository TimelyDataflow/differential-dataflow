-- Publishes positional input 0 (edge pairs) as the named trace "edges",
-- keyed by source. A consumer installed later can `import "edges"`.
-- input 0: (src, dst ;)

export "edges" = input 0 | key($0[0] ; $0[1]);
