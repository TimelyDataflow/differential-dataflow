[package]
name = "mdbook"
version = "0.0.0"
edition.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
differential-dataflow = { path = "../differential-dataflow" }
timely.workspace = true
